# Examples

Run `python quickstart.py` after installing MoTRUST. It uses synthetic CPU
arrays to demonstrate adaptive semantic protection and bounded sample-mean
projection. It neither downloads data nor trains a model.

For data-backed integration and RNA recovery, follow `docs/workflows.md`
from the repository root and provide the input contracts in
`docs/data-format.md`.
